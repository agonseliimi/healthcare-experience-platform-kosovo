package com.kosovo.healthcareexperience.enums;

/** Status of a verification request submitted by a user. */
public enum VerificationStatus {
    NOT_REQUESTED,
    PENDING,
    APPROVED,
    REJECTED
}
